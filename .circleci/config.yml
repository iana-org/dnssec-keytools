version: 2.1

jobs:
  test:
    docker:
      - image: python:3.7
    steps:
      - run: apt -y update
      - run: apt -y install swig
      - checkout
      - run: make test

workflows:
  version: 2
  build_and_test:
    jobs:
      - test
